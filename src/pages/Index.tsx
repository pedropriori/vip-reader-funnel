// Update this page (the content is just a fallback if you fail to update the page)

import UpsellLanding from '@/components/UpsellLanding';

const Index = () => {
  return <UpsellLanding />;
};

export default Index;
